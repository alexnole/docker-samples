# =================================================================================
#                           MySQL Docker Environment
#
# Set these environment variables to configure your MySQL container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# MySQL settings
MYSQL_USER=                 # required: MySQL username
MYSQL_PASSWORD=             # required: MySQL user password
MYSQL_ROOT_PASSWORD=        # required: MySQL root password
MYSQL_ROOT_HOST=            # optional: Host for root access (default: %)
MYSQL_DATABASE=             # optional: Default database name (default: local)
MYSQL_ALLOW_EMPTY_PASSWORD= # optional: Allow empty password (default: yes)

# Container and image settings
PORT=           # optional: MySQL port exposed by container (default: 3306)
IMAGE_TAG=      # optional: MySQL image tag (default: latest)
CONTAINER_NAME= # optional: Container name (default: mysql)
RESTART_POLICY= # optional: Restart policy (default: always)

# Docker network and volume settings
NETWORK_NAME=     # optional: Custom network name (default: local_dbs_network)
VOLUME_DATA_NAME= # optional: Volume name for MySQL data (default: mysql_data)
