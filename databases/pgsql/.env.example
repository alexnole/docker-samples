# =================================================================================
#                          PostgreSQL Docker Environment
#
# Set these environment variables to configure your PostgreSQL container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# -------------------
# PostgreSQL settings
# -------------------

# required: PostgreSQL user password
POSTGRES_PASSWORD=

# optional: PostgreSQL username (default: postgres)
POSTGRES_USER=

# optional: Default database name (default: local)
POSTGRES_DB=

# ----------------------------
# Container and image settings
# ----------------------------

# optional: PostgreSQL port exposed by container (default: 5432)
PORT=

# optional: PostgreSQL image tag (default: latest)
IMAGE_TAG=

# optional: Container name (default: pgsql)
CONTAINER_NAME=

# optional: Restart policy (default: always)
RESTART_POLICY=

# ----------------------------------
# Docker network and volume settings
# ----------------------------------

# optional: Custom network name (default: databases_local)
NETWORK_NAME=

# optional: Volume name for PostgreSQL data (default: pgsql_data)
VOLUME_DATA_NAME=
