# =================================================================================
#                          PostgreSQL Docker Environment
#
# Set these environment variables to configure your PostgreSQL container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# PostgreSQL settings
POSTGRES_PASSWORD= # required: PostgreSQL user password
POSTGRES_USER=     # optional: PostgreSQL username (default: postgres)
POSTGRES_DB=       # optional: Default database name (default: local)

# Container and image settings
PORT=           # optional: PostgreSQL port exposed by container (default: 5432)
IMAGE_TAG=      # optional: PostgreSQL image tag (default: latest)
CONTAINER_NAME= # optional: Container name (default: pgsql)
RESTART_POLICY= # optional: Restart policy (default: always)

# Docker network and volume settings
NETWORK_NAME=     # optional: Custom network name (default: local_dbs_network)
VOLUME_DATA_NAME= # optional: Volume name for PostgreSQL data (default: pgsql_data)
