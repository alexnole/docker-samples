# =================================================================================
#                            MongoDB Docker Environment
#
# Set these environment variables to configure your MongoDB container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# ----------------
# MongoDB settings
# ----------------

# required: MongoDB user password
MONGO_INITDB_ROOT_PASSWORD=

# required: MongoDB username
MONGO_INITDB_ROOT_USERNAME=

# optional: Default database name (default: local)
MONGO_INITDB_DATABASE=

# ----------------------------
# Container and image settings
# ----------------------------

# optional: MongoDB port exposed by container (default: 27017)
PORT=

# optional: MongoDB image tag (default: latest)
IMAGE_TAG=

# optional: Container name (default: mongodb)
CONTAINER_NAME=

# optional: Restart policy (default: always)
RESTART_POLICY=

# ----------------------------------
# Docker network and volume settings
# ----------------------------------

# optional: Custom network name (default: local_dbs_network)
NETWORK_NAME=

# optional: Volume name for MongoDB data (default: mongodb_data)
VOLUME_DATA_NAME=

# optional: Volume name for MongoDB config (default: mongodb_config)
VOLUME_CONFIG_NAME=
