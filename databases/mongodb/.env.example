# =================================================================================
#                            MongoDB Docker Environment
#
# Set these environment variables to configure your MongoDB container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# MongoDB settings
MONGO_INITDB_ROOT_PASSWORD= # required: MongoDB user password
MONGO_INITDB_ROOT_USERNAME= # required: MongoDB username
MONGO_INITDB_DATABASE=      # optional: Default database name (default: local)

# Container and image settings
PORT=           # optional: MongoDB port exposed by container (default: 27017)
IMAGE_TAG=      # optional: MongoDB image tag (default: latest)
CONTAINER_NAME= # optional: Container name (default: mongodb)
RESTART_POLICY= # optional: Restart policy (default: always)

# Docker network and volume settings
NETWORK_NAME=       # optional: Custom network name (default: local_dbs_network)
VOLUME_DATA_NAME=   # optional: Volume name for MongoDB data (default: mongodb_data)
VOLUME_CONFIG_NAME= # optional: Volume name for MongoDB config (default: mongodb_config)
