# =================================================================================
#                      Oracle Database Docker Environment
#
# Set these environment variables to configure your Oracle Database container.
# For production use, review and adjust security settings.
#
# Required variables must be set for the container to start.
# You can copy this file to .env and fill in the values, or set them in your shell.
# =================================================================================

# Oracle Database settings
ORACLE_PWD=          # required: Oracle Database admin password
ORACLE_PDB=          # optional: PDB name that should be used (default: FREEPDB1)
ORACLE_CHARACTERSET= # optional: Character set for the database (default: AL32UTF8)

# Container and image settings
PORT=           # optional: Oracle Database port exposed by container (default: 1521)
IMAGE_TAG=      # optional: Oracle Database image tag (default: latest)
CONTAINER_NAME= # optional: Container name (default: oracledb)
RESTART_POLICY= # optional: Restart policy (default: always)

# Docker network and volume settings
NETWORK_NAME=     # optional: Custom network name (default: local_dbs_network)
VOLUME_DATA_NAME= # optional: Volume name for Oracle Database data (default: oracledb_data)
